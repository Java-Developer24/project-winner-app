import RevealExperience from '@/components/RevealExperience.jsx';

export const metadata = {
  title: 'Winner Reveal - Aurora',
  description: 'Discover the lucky winner with our premium 3D reveal experience',
};

export default function RevealPage() {
  return <RevealExperience />;
}